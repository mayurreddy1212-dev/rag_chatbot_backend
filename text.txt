basic plan:

Frontend (React / Vercel)
        ↓
FastAPI (Render Web Service)
        ↓
Redis (Render Managed Redis)
        ↓
Celery Worker (Render Background Worker)
        ↓
Vector Store (Redis / PG / Qdrant etc.)


why groq ?

Extremely low latency

No GPU hosting required

Perfect for agent loops

Cheap compared to OpenAI


celery : celery -A app.celery_app worker --pool=solo --loglevel=info
